---
interface Props {
  data: Record<string, unknown>;
  id: string;
}

const { data, id } = Astro.props;
const json = JSON.stringify(data);
const html = `<script type="application/ld+json" id="${id}">${json}</script>`;
---

<Fragment set:html={html} />
